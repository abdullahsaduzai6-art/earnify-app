{
  "name": "earnify",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "concurrently": "^9.1.2"
  },
  "scripts": {
    "free": "powershell -ExecutionPolicy Bypass -File scripts\\free-ports.ps1 3001 4000",
    "dev": "npm run free && concurrently -n SERVER,WEB -c blue,green \"npm run dev:server\" \"npm run dev:web\"",
    "dev:server": "npm run dev --prefix server",
    "dev:web": "npm run dev --prefix web",
    "start:server": "npm run start --prefix server",
    "start:web": "npm run start --prefix web",
    "install:all": "npm install --prefix server && npm install --prefix web"
  }
}
